#!/usr/bin/env bash
# Direnv configuration for automatic development environment loading
# This file automatically loads the Nix flake development shell when entering the directory

use flake

# Optional: Show environment status
echo "✅ Development environment loaded via direnv"